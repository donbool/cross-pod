export async function createCalendarEvent(session: { title: string; start: string; end: string; }) {
  // TODO: Implement Google Calendar OAuth and event creation
  return { eventId: 'demo-event-id' };
} 